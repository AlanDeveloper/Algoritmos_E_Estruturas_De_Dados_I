# Comando para rodar

> gcc exerc1.c -o ex1 && ./ex1
OBS: Utilizo esse comando no terminal linux, no windows só terá que separar a expressão a partir dos && e colar os comandos separadamente

# Questão : A ausência restrição para inserção e remoção de elementos caracteriza qual estrutura de dados estudada ?

Como a própria questão sugere o uso é irrestrito já que não existe regra, logo podemos caracterizar essa implementação como uma Lista podendo inserir e remover elementos
em qualquer posição